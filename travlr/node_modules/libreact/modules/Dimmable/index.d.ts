import * as React from 'react';
import { IDimmableProps } from './index';
import { IDimmerProps } from '../Dimmer';
export interface IDimmableProps extends IDimmerProps {
    dim?: boolean;
    blur?: number;
    renderOverlay?: (dim?: boolean) => React.ReactChild;
}
export declare class Dimmable extends React.Component<IDimmableProps> {
    static defaultProps: {
        blur: number;
    };
    render(): React.ReactElement<any>;
}
