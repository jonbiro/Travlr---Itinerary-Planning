import { Component } from 'react';
import { h } from '../util';
import renderProp from '../util/renderProp';
export class ShouldUpdate extends Component {
    shouldComponentUpdate(props) {
        return this.props.when(props.props, this.props.props);
    }
    render() {
        return renderProp(this.props, this.props.props);
    }
}
export const shouldUpdate = (when) => (Comp) => (props) => h(ShouldUpdate, { when, props }, h(Comp, props));
