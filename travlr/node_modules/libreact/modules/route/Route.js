import render from '../util/renderProp';
import Match from './Match';
import { h } from '../util';
const Route = props => h(Match, props, data => data.matches
    ? render(props, data)
    : null);
export default Route;
