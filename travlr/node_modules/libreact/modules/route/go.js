import { isClient } from '../util';
const go = (page, { replace, title, state } = {}) => {
    if (isClient) {
        history[replace ? 'replaceState' : 'pushState'](state, title || '', page);
    }
};
export default go;
