export default function createMatcher(match, exact) {
    if (typeof match === 'function') {
        return match;
    }
    let regex = typeof match === 'string'
        ? new RegExp(`^(${match}${exact ? '$' : ''})`)
        : match;
    return (route) => route.match(regex);
}
