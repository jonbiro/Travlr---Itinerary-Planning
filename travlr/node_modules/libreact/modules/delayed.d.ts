import { ILazyParams, ILazyComponent } from './lazy';
export interface IDelayedParams<TProps> extends ILazyParams<TProps> {
    delay?: number;
    draf?: boolean;
    idle?: boolean;
}
export declare type TDelayed = <TProps>(params: IDelayedParams<TProps>) => ILazyComponent<TProps>;
export declare const delayed: TDelayed;
