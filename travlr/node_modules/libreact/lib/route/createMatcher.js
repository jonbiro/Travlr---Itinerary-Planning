"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function createMatcher(match, exact) {
    if (typeof match === 'function') {
        return match;
    }
    var regex = typeof match === 'string'
        ? new RegExp("^(" + match + (exact ? '$' : '') + ")")
        : match;
    return function (route) { return route.match(regex); };
}
exports.default = createMatcher;
//# sourceMappingURL=createMatcher.js.map