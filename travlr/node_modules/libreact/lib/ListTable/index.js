"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var tslib_1 = require("tslib");
var React = tslib_1.__importStar(require("react"));
var h = React.createElement;
var defaultRenderRow = function (cells) { return h.apply(void 0, ['tr', null].concat(cells)); };
exports.ListTable = function (_a) {
    var cols = _a.cols, renderRow = _a.renderRow, children = _a.children, rest = tslib_1.__rest(_a, ["cols", "renderRow", "children"]);
    var list = React.Children.toArray(children);
    var rows = [];
    var length = list.length;
    var rowLength = Math.ceil(length / cols);
    var i = 0;
    for (var m = 0; m < rowLength; m++) {
        var cells = [];
        for (var n = 0; n < cols; n++) {
            cells.push(h('td', null, i < length ? list[i] : null));
            i++;
        }
        rows.push(renderRow(cells));
    }
    return h('table', rest, h.apply(void 0, ['tbody', null].concat(rows)));
};
exports.ListTable.defaultProps = {
    cols: 3,
    renderRow: defaultRenderRow,
};
//# sourceMappingURL=index.js.map