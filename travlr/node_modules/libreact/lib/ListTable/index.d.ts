import * as React from 'react';
export interface Props extends React.AllHTMLAttributes<any> {
    cols?: number;
    renderRow?: (cells: React.ReactElement<any>[]) => React.ReactElement<any>;
}
export declare const ListTable: React.SFC<Props>;
